[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"

# Note: This is a static deployment
# Backend features (photo upload, position sync) are disabled
# Photos are served statically from public/photos folder
